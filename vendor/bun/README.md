# vendor/bun

Offline Bun runtime binaries used when tools expect `bun` on PATH.

Current bundle: `win-x64/bun.exe` (refresh: `npm run vendor:fetch-bun`).

After `npm link`, the package bin shim exposes `bun` pointing here.
